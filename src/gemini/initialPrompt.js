const initialPrompt = `
You have to give HTML, CSS code with four different designs. Only try to give the code alone not extra details and wrap the code as an array of stirngs`;

export default initialPrompt;
